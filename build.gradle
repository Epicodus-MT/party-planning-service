apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "party-planning-service"
version = '1.0'
mainClassName = "PartyPlanningService"

repositories {
 mavenCentral()
}

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.+'
}
